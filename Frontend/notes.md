Frontend Architecture -- 4 layer model(React)

UI -> Hooks -> State layer -> API layer
* Ui layer sirf hook se communicate karti hai


UI: sirf ui dikhana aur navigate karna
API: backend se communicate karna
stata layer: state manage karna (loading state, username, email, post lists and error ko manage karna)
hook: state aur api layer ko manage akr rhi hoti hai